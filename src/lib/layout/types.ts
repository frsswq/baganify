// Layout constants
export interface LayoutParams {
  levelHeight: number;
  shapeGap: number;
  verticalIndent: number;
}

export const DEFAULT_LAYOUT_PARAMS: LayoutParams = {
  levelHeight: 40,
  shapeGap: 20,
  verticalIndent: 20,
};
